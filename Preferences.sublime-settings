{
    // 在 C++ 中输入 . -> :: 后自动弹出成员补全（若用户设置里已自定义
    // auto_complete_triggers，则以用户设置为准）
    "auto_complete_triggers": [
        {
            "selector": "source.c++, source.c",
            "characters": ".>:"
        }
    ]
}
